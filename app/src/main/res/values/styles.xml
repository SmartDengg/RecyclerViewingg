<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:listDivider">@drawable/divider_purple</item>
    <item name="android:windowAnimationStyle">@style/activity_animation</item>
  </style>

  <!-- Base application theme. -->
  <style name="AppTheme.WhiteDivider" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:listDivider">@drawable/divider_white</item>
  </style>

  <style name="activity_animation" parent="@android:style/Animation.Activity">

    <item name="android:activityOpenEnterAnimation">@anim/activity_open_translate</item>
    <item name="android:activityOpenExitAnimation">@anim/activity_close_scale</item>

    <item name="android:activityCloseEnterAnimation">@anim/activity_open_scale</item>
    <item name="android:activityCloseExitAnimation">@anim/activity_close_translate</item>
  </style>

  <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
    <!-- This would set the toolbar's background color -->
    <item name="android:background">@color/colorPrimary</item>
    <!-- android:textColorPrimary is the color of the title text in the Toolbar  -->
    <item name="android:textColorPrimary">@android:color/white</item>
    <!-- android:actionMenuTextColor is the color of the text of action (menu) items  -->
    <item name="actionMenuTextColor">@android:color/holo_green_light</item>
    <!-- Enable these below if you want clicking icons to trigger a ripple effect -->
    <!--
    <item name="selectableItemBackground">?android:selectableItemBackground</item>
    <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
    -->
  </style>

  <!-- This configures the styles for the title within the Toolbar  -->
  <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
    <item name="android:layout_marginLeft">@dimen/material_16dp</item>
    <item name="android:textSize">21sp</item>
    <item name="android:textStyle">italic</item>
  </style>

  <style name="button_style">
    <item name="android:layout_width">match_parent</item>
    <item name="android:layout_height">wrap_content</item>
    <item name="android:layout_marginLeft">@dimen/material_24dp</item>
    <item name="android:layout_marginRight">@dimen/material_24dp</item>
    <item name="android:layout_marginTop">@dimen/material_8dp</item>
    <item name="android:layout_marginBottom">@dimen/material_8dp</item>
    <item name="android:background">@drawable/button_selector</item>
  </style>

</resources>
